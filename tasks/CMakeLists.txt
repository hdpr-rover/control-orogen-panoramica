# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(panoramicaTaskLib)
ADD_LIBRARY(${PANORAMICA_TASKLIB_NAME} SHARED 
    ${PANORAMICA_TASKLIB_SOURCES})
add_dependencies(${PANORAMICA_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${PANORAMICA_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${PANORAMICA_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${PANORAMICA_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${PANORAMICA_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${PANORAMICA_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${PANORAMICA_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${PANORAMICA_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${PANORAMICA_TASKLIB_HEADERS}
    DESTINATION include/orocos/panoramica)

